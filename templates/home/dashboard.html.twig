{% extends 'base.html.twig' %}

{% block title %}Панель{% endblock %}

{% block body %}
    {{ include('home/partials/_organizations_table.html.twig') }}

    {# Модальное окно быстрого редактирования организации (organizations-crud) #}
    {{ include('organization/_edit_modal.html.twig') }}

    {# Модальное окно быстрого редактирования контакта (contacts-crud) #}
    {{ include('contact/_edit_modal.html.twig') }}

    {# Модальное окно быстрого создания контакта (fix contacts-crud) #}
    {{ include('contact/_create_modal.html.twig', { organizations: organizations }) }}

    {# Модальное окно быстрого редактирования звонка (calls-crud) #}
    {{ include('call/_edit_modal.html.twig', {
        isAdmin: isAdmin,
        users: users,
        mailingCampaigns: mailingCampaigns,
        contactsByOrganization: contactsByOrganization,
    }) }}
{% endblock %}