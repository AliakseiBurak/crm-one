## 1. Базовое тело письма

- [ ] 1.1 Создать `templates/emails/campaign_base_body.html.twig` с разметкой тела по умолчанию: таблица `email-wrapper` → `email-container` (600px) → `td.email-content` с абзацем-заглушкой и `td.email-footer` с ОДО «Центр Обучающих Технологий», адресом, тремя телефонами, ссылкой на каталог, логотипом и ссылкой «Отписаться от рассылки» на токене `{{unsubscribe_url}}`. Проверить: в файле нет doctype, `<head>`, `<body>` и tracking-pixel, а классы `email-*` совпадают с теми, что использует `inline_css` в `templates/emails/campaign.html.twig`
- [ ] 1.2 Заменить в базовом теле ссылку на каталог на `https://trainingcenter.by/catalog`, чтобы все ссылки дефолта были со схемами `https`, `mailto` или `tel` — единственными, которые сохраняет санитайзер тела. Конфигурацию `allowed_link_schemes` не менять. Проверить: `grep -oE 'href="[a-z]+:' templates/emails/campaign_base_body.html.twig | sort -u` показывает только `https:`, `mailto:` или `tel:`, а `git diff config/packages/html_sanitizer.yaml` пуст
- [ ] 1.3 Добавить функциональный тест в `tests/Functional/Service/CampaignBodySanitizerTest.php`: отрендеренное базовое тело после `CampaignBodySanitizer::sanitize()` совпадает с исходным и не пусто. Проверить: `make test -- --filter CampaignBodySanitizerTest` зелёный; тест падает, если в базовое тело добавить элемент, атрибут или CSS-свойство вне allowlist

## 2. Шелл письма

- [ ] 2.1 Удалить блок `td.email-footer` из `templates/emails/campaign.html.twig`, оставив doctype, `<head>` с `<style>` и `<title>`, скрытый прехедер, `td.email-content` с `{{ bodyHtml|raw }}` и tracking-pixel. Проверить: `grep -c 'email-footer' templates/emails/campaign.html.twig` возвращает только вхождение в `<style>`
- [ ] 2.2 Обновить докблок `src/Service/CampaignEmailRenderer.php`: он больше не описывает футер как часть шелла, роль рендерера — шелл, токены, `inline_css` и текстовая часть. Проверить: `make stan` зелёный
- [ ] 2.3 Проверить, что `applyRequest()` и `previewLive()` в `src/Controller/CampaignController.php` не требуют изменений, и прогнать существующие тесты рендерера. Проверить: `make test -- --filter CampaignEmailRenderer` зелёный

## 3. Предзаполнение формы

- [ ] 3.1 В `CampaignController` при рендере формы создания (`GET /campaigns/new`) передавать `bodyDefault` — результат рендера `emails/campaign_base_body.html.twig`; при редактировании и после неудачной валидации передавать тело из запроса. Проверить: юнит-тест контроллера или функциональный тест подтверждает, что после неудачного POST тело в форме равно введённому пользователем, а не дефолту
- [ ] 3.2 В `templates/campaign/form.html.twig` заменить `{{ campaign.body|default('') }}` на `{{ bodyDefault|default('') }}`. Проверить: на `/campaigns/new` поле предзаполнено, на `/campaigns/{id}/edit` — сохранённое тело
- [ ] 3.3 Проверить, что валидация тела не ослаблена: очистка поля по-прежнему даёт «Текст письма обязателен», а тело только из запрещённых элементов — «Текст письма не содержит допустимого содержимого». Проверить: `make test -- --filter CampaignController` зелёный

## 4. Редактор

- [ ] 4.1 Прогнать консолидированный round-trip тест `все разрешённые элементы и атрибуты переживают round-trip редактора` на разметке базового тела, не меняя схему TipTap. Проверить: раскладка 600px, футер, логотип и ссылки переживают `source → visual → source`
- [ ] 4.2 Если round-trip выявил потерю разметки базового тела — расширить данные существующего теста, а не создавать второй; `assets/js/campaign-editor.js` менять только если потеря подтверждена. Проверить: e2e-тест редактора зелёный

## 5. E2E-тесты

- [ ] 5.1 В `e2e/tests/campaigns-create.spec.ts` обновить сценарий «создание рассылки с валидацией обязательных полей»: очищать предзаполненное тело перед отправкой, чтобы «Текст письма обязателен» всё ещё проверялся. Проверить: тест зелёный и по-прежнему проверяет ошибку
- [ ] 5.2 В `e2e/tests/campaigns-create.spec.ts` добавить сценарий «форма создания предзаполнена базовым шаблоном»: проверить наличие в поле футера, логотипа и ссылки «Отписаться от рассылки», затем создать рассылку, открыть предпросмотр, убедиться в футере и рабочей ссылке отписки и удалить рассылку. Проверить: тест зелёный, общая БД фикстур не изменилась
- [ ] 5.3 В `e2e/tests/campaigns-create.spec.ts` добавить сценарий «подпись и телефоны меняются, ссылка отписки удаляется»: изменить название компании и телефон в предзаполненном футере, удалить ссылку отписки, сохранить, проверить в предпросмотре изменённые данные и отсутствие ссылки отписки, удалить рассылку. Проверить: тест зелёный
- [ ] 5.4 В `e2e/tests/campaigns-preview.spec.ts` переписать «страница предпросмотра отдаёт email-документ с демо-токенами»: создавать рассылку с базовым шаблоном вместо перезаписанного тела, чтобы проверки `table[width="600"]`, «Центр Обучающих Технологий» и логотипа проверяли тело, а не шелл. Проверить: тест зелёный
- [ ] 5.5 Прогнать весь e2e-набор кампаний. Проверить: `campaigns-create`, `campaigns-editor`, `campaigns-preview`, `campaigns-crud`, `campaigns-recipients` зелёные

## 6. Документация и качество

- [ ] 6.1 Поправить `adr/0014` (решение 3): шелл больше не содержит футер, футер — редактируемое содержимое тела. Добавить дату и пометку о поправке в стиле ADR-0011. Проверить: ADR не противоречит `specs/campaigns/spec.md` и design.md
- [ ] 6.2 Прогнать проверки качества. Проверить: `make cs-dry`, `make stan`, `make test` зелёные
- [ ] 6.3 Собрать ассеты, если затронуты `assets/`. Проверить: `make styles` (или `npm run build`) завершается успешно; если ассеты не менялись — шаг не выполняется
- [ ] 6.4 Проверить ограничение отката из design.md: откат чистый, пока не было сохранений рассылки с базовым телом. Проверить: зафиксировано в описании change, а не в коде
