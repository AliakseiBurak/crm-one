## 1. Базовое тело письма

- [x] 1.1 Создать `templates/emails/campaign_base_body.html.twig` с редактируемой частью письма по умолчанию: абзац-контент и футер с ОДО «Центр Обучающих Технологий», адресом, тремя телефонами как `tel:`-ссылками, ссылкой на каталог, логотипом и ссылкой «Отписаться от рассылки» на токене `{{unsubscribe_url}}`. Раскладки письма в шаблоне нет — ею владеет шелл. Проверено: в файле нет doctype, `<head>`, `<body>`, tracking-pixel и `width: 600px`; презентация несётся инлайн-стилями, потому что санитайзер не пропускает `class`
- [x] 1.2 Все ссылки дефолта должны переживать санитизацию. Проверено: ссылки дефолта — `tel:` (3 шт.) и `https:` (каталог) плюс токен `{{unsubscribe_url}}`; все сохраняют `href` (см. группу 7 и `testBaseEmailTemplateKeepsOnlyAllowedLinkSchemes`)
- [x] 1.3 Добавить функциональный тест в `tests/Functional/Service/CampaignBodySanitizerTest.php`: базовое тело не теряет ни одного элемента, `href`, `src`, CSS-свойства и текста, а повторная санитизация идемпотентна. Проверено: тест падает на мутациях `position: fixed`, `<script>` и `javascript:` в дефолте

## 2. Шелл письма

- [x] 2.1 Удалить блок `td.email-footer` и правила `.email-footer` из `templates/emails/campaign.html.twig`, оставив doctype, `<head>` с `<style>` и `<title>`, скрытый прехедер, раскладку 600px, `td.email-content` с `{{ bodyHtml|raw }}` и tracking-pixel. Проверено: `email-footer` не встречается вовсе; в письме ровно одна раскладка (`padding: 24px 12px` один раз)
- [x] 2.2 Обновить докблок `src/Service/CampaignEmailRenderer.php`: шелл больше не описывает футер, роль рендерера — оболочка, токены, `inline_css` и текстовая часть. Проверено: `phpstan` — OK
- [x] 2.3 Обновить тесты рендерера, кодировавшие старую архитектуру (`testInlinesShellCss`, `testUnsubscribeUrlAppearsInBodyAndFooter`, `testRenderDemoUsesFixedValuesAndBodyOverride`) и добавить проверки нового контракта. Проверено: `php bin/phpunit --filter CampaignEmailRenderer` — OK (14 тестов)

## 3. Предзаполнение формы

- [x] 3.1 Добавить `App\Service\CampaignBaseBody` и передавать в форму `bodyDefault`: базовое тело при создании, сохранённое тело при редактировании и после неудачной валидации. Проверено: `testCreateValidationErrorKeepsUserBodyInsteadOfBaseTemplate` — введённое тело не затирается дефолтом
- [x] 3.2 В `templates/campaign/form.html.twig` заменить `{{ campaign.body|default('') }}` на `{{ bodyDefault|default('') }}`. Проверено: `testCreateFormIsPrefilledWithBaseBodyTemplate` и `testEditFormShowsStoredBodyInsteadOfBaseTemplate`
- [x] 3.3 Проверить, что валидация тела не ослаблена: очистка поля по-прежнему даёт «Текст письма обязателен», а тело только из запрещённых элементов — «Тело письма не содержит допустимого содержимого». Проверено: `php bin/phpunit` — 471 тест, OK

## 4. Редактор

- [x] 4.1 Прогнать round-trip редактора на предзаполненном базовом теле: раскладка 600px, футер, логотип и ссылки переживают `source → visual → source`. Код теста `базовое тело письма переживает round-trip редактора` в `e2e/tests/campaigns-editor.spec.ts` написан и типизирован; прогнан владельцем в docker-окружении, замечаний нет
- [x] 4.2 Round-trip базового тела потерь не выявил: `assets/js/campaign-editor.js` не менялся. См. 8.6 — тест упал не на потере разметки, а на слишком узких локаторах

## 5. E2E-тесты

- [x] 5.1 В `e2e/tests/campaigns-create.spec.ts` обновить сценарий «создание рассылки с валидацией обязательных полей»: очищать предзаполненное тело через новый хелпер `clearCampaignBody`. Код написан и прогнан владельцем (см. 4.1)
- [x] 5.2 Добавить сценарий «форма создания предзаполнена базовым шаблоном письма»: наличие раскладки, футера, логотипа и ссылки отписки в поле, затем создание, предпросмотр и удаление рассылки. Код написан и прогнан владельцем (см. 4.1)
- [x] 5.3 Добавить сценарий «подпись и телефоны меняются, ссылка отписки удаляется»: правка предзаполненного футера, сохранение, проверка в предпросмотре, удаление рассылки. Код написан и прогнан владельцем (см. 4.1)
- [x] 5.4 В `e2e/tests/campaigns-preview.spec.ts` переписать «страница предпросмотра отдаёт email-документ с демо-токенами»: создавать рассылку с базовым шаблоном (`createCampaignWithBaseBody`), а раскладку 600px искать по инлайн-стилю, а не по атрибуту `width` шелла. Код написан и прогнан владельцем (см. 4.1)
- [x] 5.5 Прогнать весь e2e-набор кампаний: `campaigns-create`, `campaigns-editor`, `campaigns-preview`, `campaigns-crud`, `campaigns-recipients`. Прогнан владельцем

## 6. Документация и качество

- [x] 6.1 Поправить `adr/0014`: решение 3 (футер принадлежит телу) и решение 2 (развёрнутая модель санитизации), с датой и пометкой о поправке. Проверено: не противоречит `specs/campaigns/spec.md` и design.md
- [x] 6.2 Прогнать проверки качества. Проверено: `php-cs-fixer --dry-run` — 0 файлов, `phpstan analyse` — No errors, `phpunit` — 471 тест / 1733 утверждения, OK
- [x] 6.3 Собрать ассеты: изменён `assets/scss/components/form-field.scss` (`.field__warning`). Проверено: `npm run build` — compiled successfully, класс присутствует в `public/build/app.*.css`
- [x] 6.4 Зафиксировать ограничение отката: откат чистый, пока не было сохранений рассылки с базовым телом; после отката нужна пересохранка. Зафиксировано в design.md «Migration Plan» и в Risks, не в коде

## 7. Модель санитизации тела (по решению владельца продукта)

- [x] 7.1 Развернуть `html_sanitizer.sanitizer.campaign_body` в модель «разрешено почти всё, запрещено опасное»: `allow_safe_elements: true`, явный `drop_elements`, `allow_attributes: {style: '*'}`, `drop_attributes` для legacy-presentation. Проверено: обработчики `on*` отсутствуют в безопасном наборе W3C (0 из 177 атрибутов), `script`/`iframe`/`svg` отбрасываются штатно
- [x] 7.2 Разрешить любые ссылки: широкий `allowed_link_schemes` + `allow_relative_links: true`, чтобы `{{unsubscribe_url}}` и вставленные URL сохранялись. Проверено: `href="{{unsubscribe_url}}"` переживает сохранение; `javascript`/`vbscript`/`data` исключены явно
- [x] 7.3 Сохранить инвариант «allowlist ⊆ схема редактора»: выбросить `pre`, `thead`, `tfoot` и legacy-атрибуты. Проверено: существующие тесты санитайзера остались зелёными без правки ожиданий
- [x] 7.4 Предупреждать пользователя о потерянной разметке: `CampaignBodySanitizer::removedElements()`, flash после сохранения и текст под полем при ошибке валидации. Проверено: `testCreateWarnsAboutRemovedElements` и `testCreateWithoutRemovedElementsShowsNoWarning`
- [x] 7.5 Обновить требование «Санитизация HTML тела письма» в дельте спеки под новую модель и поправить ADR-0014. Проверено: `openspec validate --strict` — valid

## 8. Демо-данные и тел-ссылки

- [x] 8.1 Оформить телефоны в футере базового тела кликабельными `tel:`-ссылками. Проверено: `testBaseEmailTemplateKeepsOnlyAllowedLinkSchemes` требует `tel:+375296848426`, `tel:+375295448426`, `tel:+375173958427`; в письме 3 ссылки
- [x] 8.2 Убрать двойную раскладку: раскладкой 600px владеет шелл, базовое тело содержит только контент и футер. Проверено: в письме `padding: 24px 12px` встречается ровно один раз; добавлен сценарий «Раскладка не дублируется телом»
- [x] 8.3 Перевести тела всех рассылок `AppFixtures` на `CampaignBaseBody`, чтобы демо-письма несли тот же футер. Проверено: `doctrine:fixtures:load` — у всех 6 рассылок по 3 `tel:`-ссылки, логотип и токен отписки; собственный контент «Акции» сохранён, приветствие не задвоено
- [x] 8.4 Добавить `CampaignBaseBodyTest` на дефолтный контент, переопределение контента и отсутствие раскладки в шаблоне. Проверить: `php bin/phpunit` — 475 тестов, OK
- [x] 8.5 Существующих кампаний в базе нет, поэтому миграция данных не требуется (решение владельца: фикстуры только, D6). Проверено: `doctrine:fixtures:load` — у всех 6 рассылок футер с 3 `tel:`-ссылками, логотипом и токеном отписки

## 9. Правки e2e по результатам прогона владельцем

- [x] 9.1 `campaigns-create.spec.ts`: удаление ссылки отписки искало `<a href=` первым атрибутом, но редактор нормализует порядок в `target/rel/href/style` — regex сделан независимым от порядка, добавлена проверка отсутствия `{{unsubscribe_url}}`
- [x] 9.2 `campaigns-editor.spec.ts` + `campaigns-preview.spec.ts`: локаторы вида «единственная img в редакторе» и «card img» стали строгими по нескольким элементам, потому что в форме лежит логотип базового шаблона — все переведены на поиск по `src`
- [x] 9.3 `campaigns-editor.spec.ts`: «недопустимый URL изображения» проверял `img` count 0, а логотип в теле есть — проверяется отсутствие именно `img[src^="javascript:"]`
- [x] 9.4 `campaigns-editor.spec.ts`: «форматирование в визуальном режиме» набирало текст поверх дефолта, из-за чего Ctrl+A выделял всё тело и давал 12 `strong` — тест сначала заменяет тело своим абзацем
- [x] 9.5 `campaigns-editor.spec.ts`: проверка текста футера больше не зависит от типографских кавычек-ёлочек, которые нормализуются редактором
- [x] 9.6 `campaigns-preview.spec.ts`: проверка картинки на карточке рассылки переведена на поиск по `src` (на карточке теперь всё тело письма вместе с логотипом) — иначе упала бы сразу после исправления 9.2
- [x] 9.7 Прогнать исправленные наборы: `campaigns-create`, `campaigns-editor`, `campaigns-preview`. Проверяет владелец

## 10. Опечатка в футере и ложнопроходящая проверка

- [x] 10.1 В `campaign_base_body.html.twig` видимая строка футера заканчивалась на `}` вместо `»` (U+007D вместо U+00BB) — из-за этого не срабатывала замена подписи в e2e. Проверено: в обеих строках (видимый текст и alt логотипа) символ U+00BB
- [x] 10.2 Ложнопроходящая проверка: `assertStringContainsString` по сырому HTML находил название компании в `alt` логотипа, а не в видимом футере, поэтому опечатка 10.1 проходила в phpunit. Утверждение переведено на видимый текст (без тегов и атрибутов). Проверено мутацией: с `}` тест падает, без неё — зелёный
- [x] 10.3 `contact-ismain.spec.ts` не связан с этим change: три теста файла назначают и снимают `isMain` у одного контакта общей фикстуры, а `fullyParallel` запускает тесты внутри файла параллельно — проверка формы организации видела «Основной» уже снятым. Файл переведён в `mode: 'serial'`
- [x] 10.4 Прогнать `contact-ismain` и `campaigns-create` после правок. Проверяет владелец
