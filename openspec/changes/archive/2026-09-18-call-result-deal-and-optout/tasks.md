## 1. Сущности и миграция

- [x] 1.1 Добавить поле `isRefusal` (boolean, default false) в Doctrine-сущность `Call`
- [x] 1.2 Добавить поля `isActive` (boolean, default true), `isOptedOut` (boolean, default false), `optOutReason` (text, nullable), `optedOutAt` (datetime, nullable) в Doctrine-сущность `Organization`
- [x] 1.3 Сгенерировать миграцию (`make:migration`) и проверить SQL
- [x] 1.4 Накатить миграцию и проверить `diff` — чистый

## 2. Автоотписка при отказе

- [x] 2.1 В сервисе/хендлере сохранения звонка: при `isRefusal === true` и `madeAt !== null` и отмеченном чекбоксе «отметить отказ организации от рассылок» → установить `Organization.isOptedOut = true` и опционально `optOutReason` (`optedOutAt` проставляется сеттером); без чекбокса — только фиксация `isRefusal`
- [x] 2.2 Добавить флеш-сообщение после автоотписки: «Организация отписана от рассылок»

## 3. Форма звонка — отказ

- [x] 3.1 Добавить чекбокс «отказ» (`isRefusal`) в CallFormType
- [x] 3.2 Обновить Twig-шаблон формы звонка: чекбокс отказа с условными чекбоксами «неактивная» и «отказ от рассылок»
- [x] 3.3 Обновить модальное окно быстрого редактирования — те же поля

## 4. Эндпоинт POST /organizations/{id}/opt-out

- [x] 4.1 Создать контроллер `OrganizationOptOutController` с маршрутом `POST /organizations/{id}/opt-out`
- [x] 4.2 Принимать JSON `{"reason": string|null}` через Request
- [x] 4.3 Проверка доступа: `OrganizationAccessVoter` (админ — всегда, менеджер — только в области доступа)
- [x] 4.4 Установить `isOptedOut = true`, `optOutReason` и `optedOutAt`; возвращать JSON 200 (или 403/404)
- [x] 4.5 Идемпотентность: повторный вызов не вызывает ошибку, обновляет причину

## 5. Тесты

- [x] 5.1 Unit-тест: `Call::setIsRefusal(true)` — корректная работа сеттера
- [x] 5.2 Unit-тест: `Organization::setIsOptedOut(false)` — сбрасывает `optOutReason` и `optedOutAt` в null
- [x] 5.3 Функциональный тест: создание звонка с отказом и чекбоксом «отметить отказ организации от рассылок» — `Organization.isOptedOut = true`
- [x] 5.4 Функциональный тест: `POST /organizations/{id}/opt-out` — успешная отписка
- [x] 5.5 Функциональный тест: `POST /organizations/{id}/opt-out` — 403 для недоступной организации
- [x] 5.6 Функциональный тест: `POST /organizations/{id}/opt-out` — 404 для несуществующей организации
- [x] 5.7 Функциональный тест: звонок с отказом без `madeAt` — отписка не происходит
- [x] 5.8 Запустить все тесты и убедиться в зелёном статусе

## 6. Фикстуры и документация

- [x] 6.1 Обновить датафикстуры — организация с `isActive = false`, организации с `isOptedOut`/`optedOutAt` (в разные периоды: прошлая неделя, текущий месяц), звонок с `isRefusal`
- [x] 6.2 Обновить ER-диаграмму (`openspec/design/er.md`) — блок Call: `is_refusal`; блок Organization: `is_active`, `is_opted_out`, `opt_out_reason`, `opted_out_at`; токен `{{unsubscribe_url}}` в комментарии `Campaign.body` и блоке Token resolution MailingService
- [x] 6.3 Запустить `openspec validate` для change — проверить валидность

## 7. Токен отписки в письме кампании

- [x] 7.1 Зарегистрировать токен `{{unsubscribe_url}}` в MailingService/MailBuilder — резолвинг в `https://{domain}/unsubscribe/{recipient.trackingToken}`
- [x] 7.2 Создать маршрут `GET /unsubscribe/{trackingToken}` — контроллер `UnsubscribeController`
- [x] 7.3 Реализовать логику: найти CampaignRecipient по токену → установить Organization.isOptedOut = true + optOutReason = "Отписка из письма" → показать страницу подтверждения
- [x] 7.4 Обработать крайние случаи: токен не найден (404), организация уже отписана («Вы уже отписаны»)
- [x] 7.5 Кнопка «Вставить отписку» не добавляется — вместо этого отображается подсказка с перечнем токенов (включая `{{unsubscribe_url}}`) над textarea тела письма
- [x] 7.6 JavaScript для вставки токена в позицию курсора не добавляется (отменено вместе с кнопкой «Вставить отписку») — токен вставляется вручную из подсказки
- [x] 7.7 Написать функциональный тест: генерация письма с `{{unsubscribe_url}}` → URL резолвится корректно
- [x] 7.8 Написать функциональный тест: GET /unsubscribe/{token} → 200 + isOptedOut = true
- [x] 7.9 Написать функциональный тест: GET /unsubscribe/invalid-token → 404

## 8. Поле isActive организации

- [x] 8.1 Добавить поле `isActive` (boolean, default true) в Doctrine-сущность `Organization` + сеттер/геттер
- [x] 8.2 В хендлере сохранения звонка при `isRefusal === true`: обработать чекбокс «отметить как неактивную» → `Organization.isActive = false`
- [x] 8.3 Добавить чекбокс «Активна» в OrganizationFormType (create + edit)
- [x] 8.4 Обновить Twig-шаблон формы организации — чекбокс isActive
- [x] 8.5 Написать функциональный тест: создание организации с isActive = false
- [x] 8.6 Написать функциональный тест: редактирование — снять/установить isActive
- [x] 8.7 Написать функциональный тест: звонок с отказом → только неактивная (без opt-out)
- [x] 8.8 Написать функциональный тест: звонок с отказом → неактивная + opt-out

## 9. Opt-out поля в форме организации

- [x] 9.1 Добавить чекбокс `isOptedOut` («Отказ от рассылки») и поле `optOutReason` («Причина отказа», textarea) в OrganizationFormType (create + edit); `optOutReason` обёрнут в контейнер `.js-opt-out-reason`, изначально скрыт
- [x] 9.2 JavaScript: при переключении чекбокса `isOptedOut` показывать/скрывать `.js-opt-out-reason`
- [x] 9.3 Сервер: при `isOptedOut = false` в submitted data игнорировать/сбрасывать `optOutReason` в null
- [x] 9.4 JavaScript: при снятии чекбокса `isOptedOut` (переход true→false) показывать диалог подтверждения «Восстановить организацию в рассылках?»; при отмене — возвращать чекбокс в состояние true
- [x] 9.5 Функциональный тест: редактирование с отказом — `isOptedOut = true`, `optOutReason` сохранён, `optedOutAt` проставлен
- [x] 9.6 Функциональный тест: снятие отказа — подтверждение → `optOutReason = null`, `optedOutAt = null`

## 10. Фильтр отписавшихся в рассылках

- [x] 10.1 Все пути создания адресатов (ручное добавление, «Выбрать все организации», добавление по группе): пропускать организации с `isOptedOut = true`; при ручном добавлении — flash «Организация отписана от рассылок»
- [x] 10.2 MailingService: повторная проверка `isOptedOut` перед отправкой — получатель помечается `failed` с `errorMessage` «Организация отписана от рассылок», письмо не формируется
- [x] 10.3 Функциональный тест: ручное добавление отписанной организации — не добавляется, flash показан
- [x] 10.4 Функциональный тест: массовое добавление пропускает отписавшиеся организации
- [x] 10.5 Функциональный тест: отписка после добавления — получатель `failed`, письмо не отправляется

## 11. Статистика отписок на дашборде

- [x] 11.1 Блок «Отписки» на дашборде: три показателя — «прошлая неделя» (`optedOutAt` в предыдущей календарной неделе), «текущий месяц» (`optedOutAt` с 1-го числа), «всего» (`isOptedOut = true`)
- [x] 11.2 Область доступа показателей: админ — все организации, менеджер — своя область доступа (ADR-0007/0008, ADR-0011)
- [x] 11.3 Функциональный тест: показатели считаются по `optedOutAt`/`isOptedOut` в области доступа пользователя
- [x] 11.4 Добавить подметрику «Из письма» (организации с `isOptedOut = true` и `optOutReason = "Отписка из письма"`) к каждому периоду блока «Отписки» + bucket `optoutEmail` в счётчики «По организациям» (`CallRepository::ORGANIZATION_BUCKETS`)
