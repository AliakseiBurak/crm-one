## 1. Сущность и миграция

- [x] 1.1 Добавить поле `isMain` (boolean, default false) в Doctrine-сущность `Contact`
- [x] 1.2 Сгенерировать миграцию (`make:migration`) и проверить SQL
- [x] 1.3 Накатить миграцию и проверить `diff`

## 2. Логика уникальности isMain

- [x] 2.1 Добавить `ContactRepository::resetIsMainForOrganization(Organization, exclude: Contact)` — DQL UPDATE
- [x] 2.2 В `ContactController::create` и `update`: после `applyRequest`, перед flush — вызвать `resetIsMainForOrganization` при `isMain = true`
- [x] 2.3 В `ContactController::applyRequest`: читать чекбокс isMain из формы
- [x] 2.4 Нормализация аномалии: при сохранении любого контакта организации со «сиротскими» isMain — оставлять isMain только у контакта с минимальным ID (reset остальных через `resetIsMainForOrganization(exclude: min-ID contact)`)

## 3. Форма контакта

- [x] 3.1 Добавить чекбокс «Основной контакт» (isMain) в ContactFormType
- [x] 3.2 Обновить Twig-шаблон формы создания/редактирования контакта
- [x] 3.3 Обновить отображение списка контактов на форме организации — метка «Основной» у isMain-контакта; подсветка главного независимо от позиции (effectiveMainId из контроллера)

## 4. Сортировка и отображение

- [x] 4.1 Обновить `ContactRepository::findByOrganizations` — порядок по ID (порядок добавления), без приоритета главного
- [x] 4.2 Добавить `ContactRepository::findByOrganization` (одна организация, по ID)
- [x] 4.3 Обновить `HomeController` — передавать effectiveMainId для подсветки главного (порядок по ID)
- [x] 4.4 Добавить бейдж «Основной» в `contact/_card.html.twig`
- [x] 4.5 Светло-оранжевый оттенок фона карточки главного контакта на дашборде (+ та же подсветка при наведении)
- [x] 4.6 Светло-оранжевый оттенок фона имени главного контакта на странице редактирования организации

## 5. Маршрутизация email в MailingService

- [x] 5.1 Обновить MailingService: при выборе email для CampaignRecipient — определить TO и CC по правилам isMain
- [x] 5.2 Разрешение effective main: isMain-контакт; при его отсутствии или нескольких — контакт с минимальным ID
- [x] 5.3 Если контакт не указан — TO на effective main, CC на остальные; имя получателя — название организации
- [x] 5.4 Если контакт указан с email — TO контакту, CC остальным контактам с email; имя получателя — имя контакта
- [x] 5.5 Если контакт указан без email — fallback на effective main (TO) + CC остальных; имя получателя — имя выбранного контакта

## 6. Подстановка токенов в Campaign

- [x] 6.1 Обновить `Campaign::fillTokens` — при `$contact = null` использовать название организации для `{{greeting}}` и `{{contact_name}}` (isMain для токенов не использовать)

## 7. Тесты

- [ ] 7.1 Unit-тест: смена isMain сбрасывает предыдущий основной контакт
- [ ] 7.2 Функциональный тест: создание контакта с isMain = true
- [ ] 7.3 Функциональный тест: MailingService — без указания контакта письмо уходит на isMain-контакт (TO) + CC остальных контактов с email; имя получателя — название организации
- [ ] 7.4 Функциональный тест: MailingService — письмо указанному контакту (TO) + CC остальных контактов с email; имя получателя — имя выбранного контакта
- [ ] 7.5 Функциональный тест: MailingService — fallback при пустом email у контакта (TO на effective main); имя получателя — имя выбранного контакта
- [ ] 7.6 Функциональный тест: fillTokens — без адресата `{{greeting}}` и `{{contact_name}}` с названием организации (даже при наличии isMain)
- [ ] 7.7 Функциональный тест: автосброс лишних isMain при сохранении контакта (главный — минимальный ID)
- [ ] 7.8 Функциональный тест: MailingService — без isMain письмо уходит на контакт с минимальным ID (TO) + CC остальных; имя получателя — название организации
- [ ] 7.9 Функциональный тест: главный контакт подсвечен светло-оранжевым оттенком независимо от позиции (дашборд и форма организации); бейдж «Основной» в правом верхнем углу карточки; hover — рыжая подсветка (в т.ч. у главного)
- [ ] 7.10 Запустить все тесты

## 8. Документация

- [x] 8.1 Обновить ER-диаграмму — isMain на Contact
- [x] 8.2 Запустить `openspec validate contact-ismain-email-routing`